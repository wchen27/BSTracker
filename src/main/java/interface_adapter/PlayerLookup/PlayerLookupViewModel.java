package interface_adapter.PlayerLookup;

import interface_adapter.ViewModel;

public class PlayerLookupViewModel extends ViewModel<PlayerLookupState> {
    public PlayerLookupViewModel() {
        super("user lookup");
    }
}
